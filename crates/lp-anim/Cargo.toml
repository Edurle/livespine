[package]
name = "lp-anim"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Livepine 动画系统（关键帧、bezier 插值、AnimationState）"

[dependencies]
lp-core = { path = "../lp-core" }
serde.workspace = true
